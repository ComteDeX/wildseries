{% extends 'base.html.twig' %}

{% block title %}Program{% endblock %}

{% block body %}
    <div class="container">
    <h1>Program</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ program.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ program.title }}</td>
            </tr>
            <tr>
                <th>Summary</th>
                <td>{{ program.summary }}</td>
            </tr>
            <tr>
                <th>Poster</th>
                <td>{{ program.poster }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('program_edit', {'slug': program.slug}) }}" class="btn btn-secondary btnlnk">Modifier</a>

    {{ include('program/_delete_form.html.twig') }}

    <a href="{{ path('program_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
